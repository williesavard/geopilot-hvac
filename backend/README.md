# Backend

This directory is reserved for optional GeoPilot services and APIs.

The backend must not become a required dependency for local monitoring. Service
boundaries and data contracts should be documented before implementation begins.
